#!/usr/bin/env node
// QUARANTINED — not the glass e2e.
// Run:  cd e2e && npm test
// Demo recording is out of band; do not invent side probes.
console.error('[e2e] record-follow-demo.js quarantined — use: cd e2e && npm test');
process.exit(0);
